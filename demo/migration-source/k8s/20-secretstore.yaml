apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: talhas-aws-sm
  namespace: talhas-experiments
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-west-2
      auth:
        jwt:
          serviceAccountRef:
            name: secrets-viewer
