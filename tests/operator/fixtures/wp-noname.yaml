apiVersion: poc.local/v1alpha1
kind: WorkloadPlan
metadata:
  name: test-noname
spec:
  intent: provision
  components:
    - name: web
      manifest:
        apiVersion: apps/v1
        kind: Deployment
        spec:
          replicas: 1
